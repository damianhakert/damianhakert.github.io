- data.features.each do |feature_block|
  .panel.panel-success
    .panel-heading
      %h2.panel-title
        = feature_block.title
    %table.table.compare-table
      %thead
        %tr
          %th
          %th.ce-col Community Edition
          %th.ee-col Enterprise Edition Starter
          %th.eep-col Enterprise Edition Premium
      %tbody.header.first
        - feature_block.features.each do |feature|
          %tr
            %td
              %a{href: feature.link}
                = feature.description
            %td.ce-col.text-center
              - if feature.in_ce
                %i.fa.fa-check
            %td.ee-col.text-center
              - if feature.in_ce || feature.in_ee
                %i.fa.fa-check
            %td.eep-col.text-center
              - if feature.in_ce || feature.in_ee || feature.in_eep
                %i.fa.fa-check
